import 'package:flutter/material.dart';

class LicenseFactCheckPage extends StatelessWidget {
  const LicenseFactCheckPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(child: Text('License Fact Check Page')),
      ),
    );
  }
}
