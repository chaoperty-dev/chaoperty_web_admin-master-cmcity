import 'package:flutter/material.dart';

class LicenseAttachPage extends StatelessWidget {
  const LicenseAttachPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(child: Text('License Attach Page')),
      ),
    );
  }
}
