import 'package:flutter/material.dart';

class LicenseAnnouncePage extends StatelessWidget {
  const LicenseAnnouncePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(child: Text('License Announce Page')),
      ),
    );
  }
}
