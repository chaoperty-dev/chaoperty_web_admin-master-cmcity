import 'package:flutter/material.dart';

class LicenseApprovePage extends StatelessWidget {
  const LicenseApprovePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(child: Text('License Approve Page')),
      ),
    );
  }
}
