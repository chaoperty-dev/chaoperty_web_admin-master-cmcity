import 'package:flutter/material.dart';

class LicensePaymentPage extends StatelessWidget {
  const LicensePaymentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Center(child: Text('License Payment Page')),
      ),
    );
  }
}
